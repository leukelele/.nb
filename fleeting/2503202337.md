When a program runs:
- processor fetches instruction -> decodes -> executes; aka Von Neumann architecture
    - what other architectures are there? I am aware of the microkernel but that is about it

What does it mean to "virtualize resources?" it apparently makes the system easier to use?
- virtualization = take phsyical resource -> virtual, more general, form
- bc of this, OS could also be thought of as a resource manager <= virt. allows many programs to run and there is also issue of concurrency
- CPU, memory, storage, etc. = resource

System calls allow for using the OS's provided resources i.e. OS provides std. library to apps


