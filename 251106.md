- 1134 `namesapce {}` creates an anonymous namespace; everything inside the
  namespace has **internal linkage** - that is, it is only available to the
  functions within that **translation unit** (src file).
- 1326 what is a special instruction?
  - it is able maintain user-level but able to modify the global status;
    how does that work?
- 1356 what is the difference between software and hardware virtualization?
    surprisingly depends on the platform but is generally comparable
