# basic cryptography vocabulary

**Cryptography** is the practice of securing communication by obscuring its contents to those beyond the intended recipient.

The three primary types of cryptography - asymmetric key cryptography, symmetric key cryptography, and hash functions - two of which utilize encryption keys. In this context, an **encryption key** is a string of characters that, when applied with an encryption algorithm, scrambles and alters data in a seemingly random manner. Similar to a physical key, the data is sealed (encrypted) and can only be accessed when the correct key is used to unseal it (decrypted). When a plaintext is encrypted by a key, a **ciphertext** is created.[^1] [^2] 

## asymmetric encryption

**Asymmetric encryption** involves generating two keys: a private key and a public key. The public key is shared with others, while the private key must be kept confidential.[^2]

The terms "private" and "public" are used to differentiate the two keys but are otherwise arbitrary; either key can be used as the private or public key, as long as their roles are consistent. What matters is that the keys are mathematically related. It is not algorithmically feasible to derive one key from the other. The keys are designed so that data encrypted with the public key can only be decrypted with the private key, and vice versa. The same key cannot be used for both encryption and decryption.

An example of asymmetric encryption may be as follows:

```
1. Bob encrypts a plain text using a public key provided by Alice (plain text + public key = ciphertext) 
2. Bob sends the ciphertext to Alice
3. Alice receives the ciphertext and decrypts it using Alice's private key (ciphertext - private key = plain text)
```
## symmetric encryption

**Symmetric encryption** describes cryptography that uses a single, shared key for both encryption and decryption. If the *shared secret* becomes known to unauthorized individuals, a new key must be created to maintain confidentiality.[^2]

Symmetric encryption is analogous to a physical key used in everyday life. The same key is used to both lock (encrypt) and unlock (decrypt) the door (the encryption algorithm), securing the contents inside (the data as ciphertext).[^1]

## hash functions

A **hash algorithm** converts a variable-length input string into a fixed-length output string, commonly referred to as a **digest**. The output is typically a scrambled string of characters, or ciphertext. [^3]

A good hash algorithm should be collision-resistant, meaning that any changes made to the input string should result in a different output string. Additionally, the algorithm should be irreversible, ensuring that the original input cannot be reconstructed from the output. [^3]

## references

[^1]: [This article](https://www.cloudflare.com/learning/ssl/what-is-a-cryptographic-key/) is what I used for my initial search into crytography. It exposed me to the relevant vocabulary and mindset that made for a great mental hook as I learned about this topic.
[^2]: I found this [video by prof. Messer](https://www.youtube.com/watch?v=z2aueocJE8Q) which provides good, succinct descriptions of asymmetric and symmetric key cryoptography.
[^3]: [This IBM article](https://www.ibm.com/think/topics/cryptography-types) is what I used to understand the basics of hash functions.
